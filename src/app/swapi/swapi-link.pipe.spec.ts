import { SwapiLinkPipe } from './swapi-link.pipe';

describe('SwapiLinkPipe', () => {
  it('create an instance', () => {
    const pipe = new SwapiLinkPipe();
    expect(pipe).toBeTruthy();
  });
});
